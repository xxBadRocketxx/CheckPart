// Helpers/RevitHelper.cs
using Autodesk.Revit.DB;

namespace CheckPart.Helpers
{
    public static class RevitHelper
    {
        // Bạn có thể thêm các extension methods hoặc các hàm helper ở đây sau này.
        // Ví dụ:
        // public static bool IsValidElementId(this Document doc, ElementId id)
        // {
        //     return doc.GetElement(id) != null;
        // }
    }
}